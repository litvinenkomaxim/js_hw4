// 1. Напишите функцию max, которая сравнивает два числа и возвращает большее:
// et 30 min;
// at 10 min;

function max(a, b) {
    if (a > b) {
        return (a);
    } else {
        return (b)
    }
}
console.log(max(0, 4)); // 0